@{
    if (!Roles.IsUserInRole(WebSecurity.CurrentUserName, "Administrators")) 
    { 
        string currentUrl = Request.Url.ToString(); 
        Response.Redirect("~/users/login?sender=" + currentUrl); 
    }
}